<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect all requests to public directory
    RewriteCond %{REQUEST_URI} !^/Esey_laravel/with-laravel/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Disable directory browsing
Options -Indexes